server:
  version: v0.1
  addr: :8080
  mode: debug
  static_dir: ./public
  view_dir: ./view
  upload_dir: ./storage
  max_multipart_memory: 10

database:
  dialect: postgres
  datasource: postgresql://user:password@db:5432/dbname
  dir: sql
  table: migrations
  max_idle_conns: 2
  max_open_conns: 16
